import {StyleSheet} from 'react-native'

export default StyleSheet.create({
  container: {
    backgroundColor: '#e0e0e0',
    overflow: 'hidden',
    position: 'relative'
  },
  gradient: {
    width: '100%',
    height: '100%'
  }
})
