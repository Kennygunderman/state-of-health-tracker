# Tracked on purpose: Jest sets NODE_ENV=test, so react-native-dotenv layers this
# file over .env and @env resolves to a non-production origin on a clean checkout.
SOH_API_BASE_URL=http://localhost:3000
