USDA_BASE_URL=https://api.nal.usda.gov/fdc/v1
USDA_FOOD_API_KEY=
# Development backend origin, without the /api suffix the app appends. Debug
# builds and Jest reject production hosts, so a copied .env must never use one.
SOH_API_BASE_URL=http://localhost:3000
SOH_API_KEY=
