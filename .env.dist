# Base URL of the State of Health API (no trailing slash — "/api" is appended
# in src/constants/endpoints.ts). Local `expo run` reads this file; EAS builds
# get the value from EAS environment variables instead.
SOH_API_BASE_URL=https://stateofhealthapi.com
